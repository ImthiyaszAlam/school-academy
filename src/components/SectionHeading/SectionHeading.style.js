import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 8,
    flexDirection: 'row',
    alignItems: 'center',
  },

  text: {
    fontSize: 32,
    fontStyle: 'normal',
    fontWeight: 'bold',
    textTransform: 'capitalize',
    letterSpacing: 1,
    alignItems: 'center',
    textAlign: 'center',
    fontWeight: 'bold',
    color: '#000000',
  },
});
