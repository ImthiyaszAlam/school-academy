import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },

  text: {
    fontSize: 32,
    fontStyle: 'normal',
    fontWeight: 'bold',
    letterSpacing: 1,
    alignItems: 'center',
    textAlign: 'center',
    fontWeight: 'normal',
    letterSpacing: 0.5,
    color: '#FFD700',
  },
});
